<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tonyapps.jsonparsing">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".GoggleNews" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AssociatedPress" />
        <activity android:name=".BBC_Sport" />
        <activity android:name=".BBC_News" />
        <activity android:name=".Bild" />
        <activity android:name=".Bloomberg" />
        <activity android:name=".BusinessInsider" />
        <activity android:name=".BusinessInsiderUK" />
        <activity android:name=".Buzzfeed" />
    </application>

</manifest>